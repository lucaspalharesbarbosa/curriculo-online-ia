import { EstadosInteracaoPrototype } from "@/components/prototypes/EstadosInteracaoPrototype";

export const metadata = {
  title: "Protótipo — foco por teclado e efeito de clique",
  robots: { index: false, follow: false },
};

export default function EstadosInteracaoPrototypePage() {
  return <EstadosInteracaoPrototype />;
}
